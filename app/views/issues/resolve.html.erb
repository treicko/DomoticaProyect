<%= form_for(@issue) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :thermostat_id, :value => params[:thermostat_id] %>
  </div>
  <div class="field">
    <%= f.label :resolution %><br>
    <%= f.text_area :status %>
  </div>

  <%= f.hidden_field :status, :value => 'RESUELTO' %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>